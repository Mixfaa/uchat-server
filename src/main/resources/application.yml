spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/socket-uchat
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect